---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - .claude/get-shit-done/scenarios/software-eng/manifest.json
  - .claude/get-shit-done/scenarios/software-eng/workflows/
  - .claude/get-shit-done/scenarios/software-eng/agents/
  - .claude/get-shit-done/scenarios/software-eng/templates/
  - .claude/get-shit-done/scenarios/software-eng/references/
  - .claude/get-shit-done/scenarios/software-eng/README.md
autonomous: true
requirements:
  - CORE-04
  - SCEN-04

must_haves:
  truths:
    - "All existing GSD software engineering workflows are packaged as scenarios/software-eng/"
    - "manifest.json defines all commands, agents, templates, and references for software-eng scenario"
    - "Scenario package follows SCENARIO-STRUCTURE.md standard layout"
    - "All existing workflows continue to work from new location"
  artifacts:
    - path: ".claude/get-shit-done/scenarios/software-eng/manifest.json"
      provides: "Software-eng scenario metadata and registration"
      contains: "software-eng"
    - path: ".claude/get-shit-done/scenarios/software-eng/workflows/"
      provides: "30+ workflow files moved from .claude/get-shit-done/workflows/"
      min_lines: 1
    - path: ".claude/get-shit-done/scenarios/software-eng/agents/"
      provides: "15+ agent workflow files"
      min_lines: 1
    - path: ".claude/get-shit-done/scenarios/software-eng/templates/"
      provides: "10+ template files moved from .claude/get-shit-done/templates/"
      min_lines: 1
  key_links:
    - from: ".claude/get-shit-done/scenarios/software-eng/manifest.json"
      to: "workflows/new-project.md"
      via: "commands array registration"
      pattern: '"workflow".*"workflows/new-project.md"'
    - from: ".claude/get-shit-done/scenarios/software-eng/workflows/"
      to: ".claude/get-shit-done/core/"
      via: "workflow references to core modules"
      pattern: "@.*core/(engine|state|scheduler)"
---

<objective>
Package existing GSD software engineering workflows, agents, templates, and references as the first scenario package (scenarios/software-eng/).

Purpose: Prove the scenario package system works by migrating all current GSD functionality to the new structure while maintaining full backward compatibility.

Output: Complete scenarios/software-eng/ package with manifest.json and all existing GSD files reorganized into scenario structure.
</objective>

<execution_context>
@/Users/joffy/Site/get-shit-done-ev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joffy/Site/get-shit-done-ev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/ARCHITECTURE.md
@.planning/phases/01-foundation/MANIFEST-SPEC.md
@.planning/phases/01-foundation/SCENARIO-STRUCTURE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scenarios/software-eng directory structure and manifest.json</name>
  <files>.claude/get-shit-done/scenarios/software-eng/manifest.json, .claude/get-shit-done/scenarios/software-eng/README.md</files>

  <read_first>
    - .planning/phases/01-foundation/MANIFEST-SPEC.md (manifest.json schema and software-eng example)
    - .planning/phases/01-foundation/SCENARIO-STRUCTURE.md (directory structure specification)
    - .claude/commands/gsd/ (list all existing GSD commands to register)
    - .claude/get-shit-done/bin/lib/model-profiles.cjs (agent role list for manifest)
  </read_first>

  <action>
**Part A: Create directory structure**

Create the scenario package structure:
```bash
mkdir -p .claude/get-shit-done/scenarios/software-eng/{workflows,agents,templates,references}
```

**Part B: Generate manifest.json**

Create `.claude/get-shit-done/scenarios/software-eng/manifest.json` with complete registration of all existing GSD commands, agents, templates, and references.

Use this structure (from MANIFEST-SPEC.md):
```json
{
  "name": "software-eng",
  "version": "1.0.0",
  "description": "GSD software engineering workflow (questioning → research → roadmap → execute → verify)",
  "gsd_version": ">=1.0.0",
  "commands": [
    {"name": "new-project", "namespace": "gsd", "workflow": "workflows/new-project.md"},
    {"name": "plan-phase", "namespace": "gsd", "workflow": "workflows/plan-phase.md"},
    {"name": "execute-phase", "namespace": "gsd", "workflow": "workflows/execute-phase.md"},
    {"name": "verify-phase", "namespace": "gsd", "workflow": "workflows/verify-phase.md"},
    {"name": "research-phase", "namespace": "gsd", "workflow": "workflows/research-phase.md"},
    {"name": "discuss-phase", "namespace": "gsd", "workflow": "workflows/discuss-phase.md"},
    {"name": "complete-milestone", "namespace": "gsd", "workflow": "workflows/complete-milestone.md"},
    {"name": "new-milestone", "namespace": "gsd", "workflow": "workflows/new-milestone.md"},
    {"name": "add-phase", "namespace": "gsd", "workflow": "workflows/add-phase.md"},
    {"name": "insert-phase", "namespace": "gsd", "workflow": "workflows/insert-phase.md"},
    {"name": "remove-phase", "namespace": "gsd", "workflow": "workflows/remove-phase.md"},
    {"name": "pause-work", "namespace": "gsd", "workflow": "workflows/pause-work.md"},
    {"name": "resume-project", "namespace": "gsd", "workflow": "workflows/resume-project.md"},
    {"name": "add-todo", "namespace": "gsd", "workflow": "workflows/add-todo.md"},
    {"name": "check-todos", "namespace": "gsd", "workflow": "workflows/check-todos.md"},
    {"name": "map-codebase", "namespace": "gsd", "workflow": "workflows/map-codebase.md"},
    {"name": "autonomous", "namespace": "gsd", "workflow": "workflows/autonomous.md"},
    {"name": "validate-phase", "namespace": "gsd", "workflow": "workflows/validate-phase.md"},
    {"name": "ui-review", "namespace": "gsd", "workflow": "workflows/ui-review.md"},
    {"name": "diagnose-issues", "namespace": "gsd", "workflow": "workflows/diagnose-issues.md"},
    {"name": "debug", "namespace": "gsd", "workflow": "workflows/debug.md"},
    {"name": "cleanup", "namespace": "gsd", "workflow": "workflows/cleanup.md"},
    {"name": "stats", "namespace": "gsd", "workflow": "workflows/stats.md"},
    {"name": "settings", "namespace": "gsd", "workflow": "workflows/settings.md"},
    {"name": "help", "namespace": "gsd", "workflow": "workflows/help.md"},
    {"name": "health", "namespace": "gsd", "workflow": "workflows/health.md"}
  ],
  "agents": [
    {"role": "gsd-planner", "workflow": "workflows/plan-phase.md", "model_profile_key": "gsd-planner"},
    {"role": "gsd-roadmapper", "workflow": "workflows/new-project.md", "model_profile_key": "gsd-roadmapper"},
    {"role": "gsd-executor", "workflow": "workflows/execute-plan.md", "model_profile_key": "gsd-executor"},
    {"role": "gsd-phase-researcher", "workflow": "workflows/research-phase.md", "model_profile_key": "gsd-phase-researcher"},
    {"role": "gsd-project-researcher", "workflow": "workflows/research-phase.md", "model_profile_key": "gsd-project-researcher"},
    {"role": "gsd-research-synthesizer", "workflow": "workflows/research-phase.md", "model_profile_key": "gsd-research-synthesizer"},
    {"role": "gsd-debugger", "workflow": "workflows/debug.md", "model_profile_key": "gsd-debugger"},
    {"role": "gsd-codebase-mapper", "workflow": "workflows/map-codebase.md", "model_profile_key": "gsd-codebase-mapper"},
    {"role": "gsd-verifier", "workflow": "workflows/verify-phase.md", "model_profile_key": "gsd-verifier"},
    {"role": "gsd-plan-checker", "workflow": "workflows/plan-phase.md", "model_profile_key": "gsd-plan-checker"},
    {"role": "gsd-integration-checker", "workflow": "workflows/verify-phase.md", "model_profile_key": "gsd-integration-checker"},
    {"role": "gsd-nyquist-auditor", "workflow": "workflows/verify-phase.md", "model_profile_key": "gsd-nyquist-auditor"},
    {"role": "gsd-ui-researcher", "workflow": "workflows/ui-review.md", "model_profile_key": "gsd-ui-researcher"},
    {"role": "gsd-ui-checker", "workflow": "workflows/ui-review.md", "model_profile_key": "gsd-ui-checker"},
    {"role": "gsd-ui-auditor", "workflow": "workflows/ui-review.md", "model_profile_key": "gsd-ui-auditor"}
  ],
  "templates": [
    {"name": "project", "path": "templates/project.md"},
    {"name": "requirements", "path": "templates/requirements.md"},
    {"name": "roadmap", "path": "templates/roadmap.md"},
    {"name": "plan", "path": "templates/plan.md"},
    {"name": "summary", "path": "templates/summary.md"},
    {"name": "context", "path": "templates/context.md"},
    {"name": "discovery", "path": "templates/discovery.md"},
    {"name": "verification", "path": "templates/verification.md"},
    {"name": "uat", "path": "templates/uat.md"},
    {"name": "milestone-retrospective", "path": "templates/milestone-retrospective.md"}
  ],
  "references": [
    {"name": "questioning", "path": "references/questioning.md"},
    {"name": "git-integration", "path": "references/git-integration.md"},
    {"name": "git-planning-commit", "path": "references/git-planning-commit.md"},
    {"name": "model-profiles", "path": "references/model-profiles.md"},
    {"name": "model-profile-resolution", "path": "references/model-profile-resolution.md"},
    {"name": "verification-patterns", "path": "references/verification-patterns.md"},
    {"name": "tdd", "path": "references/tdd.md"},
    {"name": "checkpoints", "path": "references/checkpoints.md"},
    {"name": "planning-config", "path": "references/planning-config.md"},
    {"name": "continuation-format", "path": "references/continuation-format.md"},
    {"name": "phase-argument-parsing", "path": "references/phase-argument-parsing.md"},
    {"name": "decimal-phase-calculation", "path": "references/decimal-phase-calculation.md"},
    {"name": "ui-brand", "path": "references/ui-brand.md"}
  ]
}
```

**Part C: Create README.md**

Create `.claude/get-shit-done/scenarios/software-eng/README.md`:
```markdown
# Software Engineering Scenario

The default GSD scenario for software project development.

## What it provides

**Workflow:** questioning → research → roadmap → execute → verify

**Commands:** `/gsd:new-project`, `/gsd:plan-phase`, `/gsd:execute-phase`, `/gsd:verify-phase`, and 20+ more

**Agents:** 15 specialized agents (planner, executor, verifier, researcher, debugger, etc.)

**Use for:** Building software projects with structured phase planning, automated verification, and git integration.

## Directory structure

- `workflows/` — 30+ workflow orchestration files
- `agents/` — Placeholder for agent-specific workflows
- `templates/` — Document generation templates (PROJECT.md, ROADMAP.md, PLAN.md, etc.)
- `references/` — Shared knowledge documents (questioning, git integration, TDD, etc.)

## Version

1.0.0 — Initial extraction from monolithic GSD structure
```
  </action>

  <verify>
    <automated>
test -f .claude/get-shit-done/scenarios/software-eng/manifest.json && \
test -d .claude/get-shit-done/scenarios/software-eng/workflows && \
test -d .claude/get-shit-done/scenarios/software-eng/templates && \
test -d .claude/get-shit-done/scenarios/software-eng/references && \
node -e "const m = JSON.parse(require('fs').readFileSync('.claude/get-shit-done/scenarios/software-eng/manifest.json', 'utf-8')); if (m.name !== 'software-eng' || !m.commands || m.commands.length < 20) process.exit(1);" && \
grep -q "new-project" .claude/get-shit-done/scenarios/software-eng/manifest.json && \
grep -q "gsd-planner" .claude/get-shit-done/scenarios/software-eng/manifest.json && \
test -f .claude/get-shit-done/scenarios/software-eng/README.md
    </automated>
  </verify>

  <acceptance_criteria>
    - .claude/get-shit-done/scenarios/software-eng/manifest.json exists and is valid JSON
    - manifest.json has "name": "software-eng"
    - manifest.json "commands" array has at least 20 entries including "new-project", "plan-phase", "execute-phase"
    - manifest.json "agents" array includes "gsd-planner", "gsd-executor", "gsd-verifier"
    - manifest.json "templates" array includes "project", "plan", "summary"
    - manifest.json "references" array includes "questioning", "git-integration", "tdd"
    - Directory structure has workflows/, agents/, templates/, references/ subdirectories
    - README.md exists and contains "Software Engineering Scenario"
  </acceptance_criteria>

  <done>
scenarios/software-eng/ directory structure created with manifest.json defining all commands, agents, templates, and references. README.md documents the scenario. Structure follows SCENARIO-STRUCTURE.md specification.
  </done>
</task>

<task type="auto">
  <name>Task 2a: Move workflow files to scenario package</name>
  <files>.claude/get-shit-done/scenarios/software-eng/workflows/</files>

  <read_first>
    - .claude/get-shit-done/scenarios/software-eng/manifest.json (from Task 1 - workflow files to move)
    - .planning/phases/01-foundation/SCENARIO-STRUCTURE.md (path resolution rules)
    - .claude/get-shit-done/workflows/ (source directory)
  </read_first>

  <action>
Move existing GSD workflow files to scenarios/software-eng/workflows/ preserving file contents.

```bash
# Move all workflow files from workflows/ to scenarios/software-eng/workflows/
cp -r .claude/get-shit-done/workflows/*.md .claude/get-shit-done/scenarios/software-eng/workflows/
```

Expected files to move (30+ workflows):
- new-project.md, plan-phase.md, execute-phase.md, verify-phase.md
- research-phase.md, discuss-phase.md
- new-milestone.md, complete-milestone.md, plan-milestone-gaps.md, audit-milestone.md
- add-phase.md, insert-phase.md, remove-phase.md
- pause-work.md, resume-project.md
- add-todo.md, check-todos.md
- map-codebase.md, autonomous.md, validate-phase.md
- ui-review.md, diagnose-issues.md, debug.md
- cleanup.md, stats.md, settings.md, update.md
- help.md, health.md, join-discord.md
- execute-plan.md, verify-work.md
- list-phase-assumptions.md

**Important:** This is a COPY operation, not move. Original files remain in place until scenario loader is working (Plan 04). This ensures backward compatibility during transition.
  </action>

  <verify>
    <automated>
test -f .claude/get-shit-done/scenarios/software-eng/workflows/new-project.md && \
test -f .claude/get-shit-done/scenarios/software-eng/workflows/plan-phase.md && \
test -f .claude/get-shit-done/scenarios/software-eng/workflows/execute-phase.md && \
find .claude/get-shit-done/scenarios/software-eng/workflows -name "*.md" | wc -l | awk '{if ($1 >= 30) exit 0; else exit 1}'
    </automated>
  </verify>

  <acceptance_criteria>
    - .claude/get-shit-done/scenarios/software-eng/workflows/ contains at least 30 .md files
    - Workflows include new-project.md, plan-phase.md, execute-phase.md, verify-phase.md
    - Original files still exist in .claude/get-shit-done/workflows/ (copy, not move)
  </acceptance_criteria>

  <done>
All workflow files copied to scenarios/software-eng/workflows/ directory. Original files remain in place for backward compatibility during transition.
  </done>
</task>

<task type="auto">
  <name>Task 2b: Move template files to scenario package</name>
  <files>.claude/get-shit-done/scenarios/software-eng/templates/</files>

  <read_first>
    - .claude/get-shit-done/scenarios/software-eng/manifest.json (from Task 1 - template files to move)
    - .planning/phases/01-foundation/SCENARIO-STRUCTURE.md (path resolution rules)
    - .claude/get-shit-done/templates/ (source directory)
  </read_first>

  <action>
Move existing GSD template files to scenarios/software-eng/templates/ preserving file contents.

```bash
# Move all template files from templates/ to scenarios/software-eng/templates/
cp -r .claude/get-shit-done/templates/*.md .claude/get-shit-done/scenarios/software-eng/templates/
```

Expected files to move (10+ templates):
- project.md, requirements.md, roadmap.md
- plan.md, summary.md, context.md
- discovery.md, verification.md, uat.md
- milestone-retrospective.md

**Important:** This is a COPY operation, not move. Original files remain in place until scenario loader is working (Plan 04). This ensures backward compatibility during transition.
  </action>

  <verify>
    <automated>
test -f .claude/get-shit-done/scenarios/software-eng/templates/project.md && \
test -f .claude/get-shit-done/scenarios/software-eng/templates/plan.md && \
test -f .claude/get-shit-done/scenarios/software-eng/templates/summary.md && \
find .claude/get-shit-done/scenarios/software-eng/templates -name "*.md" | wc -l | awk '{if ($1 >= 10) exit 0; else exit 1}'
    </automated>
  </verify>

  <acceptance_criteria>
    - .claude/get-shit-done/scenarios/software-eng/templates/ contains at least 10 .md files
    - Templates include project.md, plan.md, summary.md
    - Original files still exist in .claude/get-shit-done/templates/ (copy, not move)
  </acceptance_criteria>

  <done>
All template files copied to scenarios/software-eng/templates/ directory. Original files remain in place for backward compatibility during transition.
  </done>
</task>

<task type="auto">
  <name>Task 2c: Move reference files to scenario package</name>
  <files>.claude/get-shit-done/scenarios/software-eng/references/</files>

  <read_first>
    - .claude/get-shit-done/scenarios/software-eng/manifest.json (from Task 1 - reference files to move)
    - .planning/phases/01-foundation/SCENARIO-STRUCTURE.md (path resolution rules)
    - .claude/get-shit-done/references/ (source directory)
  </read_first>

  <action>
Move existing GSD reference files to scenarios/software-eng/references/ preserving file contents.

```bash
# Move all reference files from references/ to scenarios/software-eng/references/
cp -r .claude/get-shit-done/references/*.md .claude/get-shit-done/scenarios/software-eng/references/
```

Expected files to move (12+ references):
- questioning.md, git-integration.md, git-planning-commit.md
- model-profiles.md, model-profile-resolution.md
- verification-patterns.md, tdd.md, checkpoints.md
- planning-config.md, continuation-format.md
- phase-argument-parsing.md, decimal-phase-calculation.md
- ui-brand.md

**Verification**

Verify all files referenced in manifest.json now exist in scenario package:
```bash
node -e "
const fs = require('fs');
const manifest = JSON.parse(fs.readFileSync('.claude/get-shit-done/scenarios/software-eng/manifest.json', 'utf-8'));
const scenarioRoot = '.claude/get-shit-done/scenarios/software-eng/';

let missing = [];

manifest.commands.forEach(cmd => {
  const path = scenarioRoot + cmd.workflow;
  if (!fs.existsSync(path)) missing.push(path);
});

manifest.templates.forEach(tpl => {
  const path = scenarioRoot + tpl.path;
  if (!fs.existsSync(path)) missing.push(path);
});

manifest.references.forEach(ref => {
  const path = scenarioRoot + ref.path;
  if (!fs.existsSync(path)) missing.push(path);
});

if (missing.length > 0) {
  console.error('Missing files:', missing);
  process.exit(1);
}

console.log('All manifest files exist in scenario package');
"
```

**Important:** This is a COPY operation, not move. Original files remain in place until scenario loader is working (Plan 04). This ensures backward compatibility during transition.
  </action>

  <verify>
    <automated>
test -f .claude/get-shit-done/scenarios/software-eng/references/questioning.md && \
test -f .claude/get-shit-done/scenarios/software-eng/references/git-integration.md && \
test -f .claude/get-shit-done/scenarios/software-eng/references/tdd.md && \
find .claude/get-shit-done/scenarios/software-eng/references -name "*.md" | wc -l | awk '{if ($1 >= 12) exit 0; else exit 1}'
    </automated>
  </verify>

  <acceptance_criteria>
    - .claude/get-shit-done/scenarios/software-eng/references/ contains questioning.md, git-integration.md, tdd.md
    - Node validation script confirms all manifest.json referenced files exist
    - Original files still exist in .claude/get-shit-done/references/ (copy, not move)
  </acceptance_criteria>

  <done>
All reference files copied to scenarios/software-eng/references/ directory. File structure matches manifest.json registration. Original files remain in place for backward compatibility during transition.
  </done>
</task>

</tasks>

<verification>
Verify scenario package is complete and valid:

```bash
# Check directory structure
ls -R .claude/get-shit-done/scenarios/software-eng/

# Validate manifest.json
node -e "JSON.parse(require('fs').readFileSync('.claude/get-shit-done/scenarios/software-eng/manifest.json', 'utf-8'))"

# Verify file counts
echo "Workflows: $(find .claude/get-shit-done/scenarios/software-eng/workflows -name '*.md' | wc -l)"
echo "Templates: $(find .claude/get-shit-done/scenarios/software-eng/templates -name '*.md' | wc -l)"
echo "References: $(find .claude/get-shit-done/scenarios/software-eng/references -name '*.md' | wc -l)"

# Verify all manifest references exist
node .claude/get-shit-done/bin/gsd-tools.cjs verify scenario-package .claude/get-shit-done/scenarios/software-eng 2>/dev/null || echo "Scenario package validation (to be implemented in Plan 04)"
```

Existing commands still work (original files untouched):
```bash
node .claude/get-shit-done/bin/gsd-tools.cjs init plan-phase "01"
```
</verification>

<success_criteria>
- scenarios/software-eng/ directory exists with manifest.json and all subdirectories
- manifest.json is valid JSON with complete command, agent, template, and reference registration
- 30+ workflow files exist in scenarios/software-eng/workflows/
- 10+ template files exist in scenarios/software-eng/templates/
- 12+ reference files exist in scenarios/software-eng/references/
- All files referenced in manifest.json exist in scenario package
- README.md documents the software-eng scenario
- Original .claude/get-shit-done/ files remain untouched (backward compatibility)
- Scenario package follows SCENARIO-STRUCTURE.md specification
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
